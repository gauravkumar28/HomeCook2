jQuery('#dialog').html('<%= escape_javascript render 'party_orders/new' %>')

jQuery( document ).ready( function() {
    jQuery( '#dialog' ).dialog( { height: 500,
    width: 400,
    modal: true,
    resizable: true,
    dialogClass: 'no-close success-dialog'} );
  });
jQuery( '#dialog' ).dialog( 'open' );