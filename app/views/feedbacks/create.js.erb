jQuery('#dialog').html('<%= escape_javascript render 'feedbacks/new' %>')

jQuery( document ).ready( function() {
    jQuery( '#dialog' ).dialog( { height: 350,
    width: 400,
    modal: true,
    resizable: true,
    dialogClass: 'no-close success-dialog'} );
  });
jQuery( '#dialog' ).dialog( 'open' );